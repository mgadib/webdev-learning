import type { LucideIcon } from "lucide-react";
import {
  Cloud,
  Layers,
  Monitor,
  Server,
  ShieldCheck,
} from "lucide-react";

export const learningPathIcons: Record<string, LucideIcon> = {
  monitor: Monitor,
  server: Server,
  layers: Layers,
  cloud: Cloud,
  "shield-check": ShieldCheck,
};
